function dibujarGraficoBarras(canvas, datos){if(!canvas)return;const ctx=canvas.getContext('2d');const w=canvas.width=canvas.clientWidth*2,h=canvas.height=canvas.clientHeight*2;ctx.scale(2,2);const W=canvas.clientWidth,H=canvas.clientHeight,pad=42;ctx.clearRect(0,0,W,H);const max=Math.max(100,...datos.map(d=>Number(d.valor)||0));ctx.strokeStyle='#d9e7ed';ctx.fillStyle='#5f747c';ctx.font='12px Arial';for(let i=0;i<=4;i++){const y=H-pad-(H-pad*2)*i/4;ctx.beginPath();ctx.moveTo(pad,y);ctx.lineTo(W-15,y);ctx.stroke();ctx.fillText(Math.round(max*i/4),8,y+4)}const bw=Math.max(18,(W-pad-20)/datos.length*.55);const gap=(W-pad-20)/datos.length;datos.forEach((d,i)=>{const v=Number(d.valor)||0;const bh=(H-pad*2)*(v/max);const x=pad+i*gap+gap/2-bw/2;const y=H-pad-bh;ctx.fillStyle='#018abd';ctx.roundRect(x,y,bw,bh,8);ctx.fill();ctx.fillStyle='#49626b';ctx.textAlign='center';ctx.fillText(String(d.etiqueta).slice(0,12),x+bw/2,H-12);ctx.fillText(v.toFixed(1),x+bw/2,y-7)});ctx.textAlign='start'}
